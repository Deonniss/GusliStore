package golovin.store.gusli.repository;

import golovin.store.gusli.entity.ProductParam;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductParamRepository extends JpaRepository<ProductParam, Long> {
}
